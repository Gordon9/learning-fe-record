import React, { useEffect, useState } from "react";

const Item = () => {
  const [item, setItem] = useState([]);

  useEffect(() => {}, []);

  return (
    <div>
      <h1>Item</h1>
    </div>
  );
};

export default Item;
